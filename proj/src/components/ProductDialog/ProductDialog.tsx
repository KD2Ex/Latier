import React from 'react';
import {Dialog, DialogTitle} from "@mui/material";

const ProductDialog = () => {
	return (
		<Dialog>

			<DialogTitle>
				
			</DialogTitle>

		</Dialog>
	);
};

export default ProductDialog;