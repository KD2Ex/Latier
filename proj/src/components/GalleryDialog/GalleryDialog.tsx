import React from 'react';
import {Dialog, DialogContent, DialogTitle} from "@mui/material";

const GalleryDialog = ({image}) => {
	return (
		<Dialog>

			<DialogContent

			>
				
			</DialogContent>
		</Dialog>
	);
};

export default GalleryDialog;